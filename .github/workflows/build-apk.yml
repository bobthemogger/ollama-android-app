name: Build OllamaApp APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Download and install Gradle 7.6.3
        run: |
          cd /tmp
          curl -sL https://services.gradle.org/distributions/gradle-7.6.3-bin.zip -o gradle-7.6.3-bin.zip
          unzip -o gradle-7.6.3-bin.zip -d /opt/gradle
          ln -sf /opt/gradle/gradle-7.6.3/bin/gradle /usr/local/bin/gradle7
          echo "Gradle installed at /opt/gradle/gradle-7.6.3"

      - name: Create Gradle Wrapper
        run: |
          cd /opt/gradle7/gradle-7.6.3
          export PATH=/opt/gradle7/gradle-7.6.3/bin:$PATH
          gradle7 wrapper --gradle-version=7.6.3 --no-daemon
          chmod +x /public/ollama-app/gradlew

      - name: Build with Gradle
        run: |
          cd /public/ollama-app
          export PATH=/opt/gradle7/gradle-7.6.3/bin:$PATH
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ollama-app-apk
          path: app/build/outputs/apk/debug/*.apk